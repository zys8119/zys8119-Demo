# 🎯 悬浮配置面板 - 快速使用指南

## 🚀 一句话总结

**配置面板现在是悬浮窗口，干净整洁，无滚动条干扰！**

## 📍 面板位置

### 桌面版本
```
┌─────────────────────┐     ┌──────────────┐
│   主界面            │     │ 悬浮配置面板  │  ← 右侧
│                     │     │              │
│   视频播放          │     │ 参数设置     │
│   水印列表          │     │ 预设配置     │
│                     │     │ 配置信息     │
└─────────────────────┘     └──────────────┘
```

### 移动版本
```
┌──────────────────┐
│   主界面         │
│                  │
│   视频播放       │
│   水印列表       │
│                  │
├──────────────────┤
│ 悬浮配置面板 ↓   │ ← 底部
│ 参数设置、预设   │
└──────────────────┘
```

## 🎮 基本操作

### 打开配置面板

```
步骤:
1. 点击 "► 显示高级配置" 按钮
2. 悬浮面板从右侧滑入 (有动画)
3. 完成！
```

### 关闭配置面板

```
方式 1: 点击面板右上的 [✕] 关闭按钮
        ↓
        面板向右滑出消失

方式 2: 点击 "▼ 隐藏高级配置" 按钮
        ↓
        面板关闭
```

## ✨ 新设计的优点

### 1️⃣ 不遮挡主界面
```
之前: 配置面板占据右侧面板位置
现在: 悬浮独立窗口，主界面完全可见
```

### 2️⃣ 无滚动条干扰
```
之前: 配置过多时有滚动条
现在: 隐藏滚动条，内容自由滚动
```

### 3️⃣ 智能响应式
```
桌面: 右侧悬浮，宽度 420px
移动: 底部悬浮，自动适应宽度
```

### 4️⃣ 快速打开/关闭
```
一键操作，平滑动画效果
```

## 📊 面板内容组成

```
┌─ 🎯 水印去除强度配置        [✕] ─┐
├──────────────────────────────────┤
│ ⭐⭐⭐⭐⭐ 极致质量              │  ← 预设按钮
│ ⭐⭐⭐⭐ 平衡推荐                │
│ ⭐⭐⭐ 快速模式                 │
│                              │
│ 算法: [Navier-Stokes ▼]      │  ← 算法选择
│                              │
│ 邻域半径: [50] ────────●───── │  ← 参数调整
│ 模糊半径: [20] ────────●───── │
│ Sigma: [50] ─────────●─────── │
│ 平滑迭代: [3] ────────●─────── │
│                              │
│ ☑ 预处理：增强对比度        │  ← 处理选项
│ ☑ 后处理：额外平滑           │
│                              │
│ 当前配置信息                 │  ← 配置信息
│ 🔴 Navier-Stokes（最强）      │
│ 邻域半径: 50 🔥 超强          │
│ 平滑迭代: 3 次               │
│ ⚠️ 警告: 参数非常强...        │
└──────────────────────────────────┘
```

## 🎯 使用场景

### 场景 1：快速预览

```
1. 上传视频
2. 框选水印
3. 点击 "► 显示高级配置"
4. 选择 "⭐⭐⭐⭐ 平衡推荐" 预设
5. 点击 [✕] 关闭面板
6. 点击 "开始处理"

时间: ~5 秒
效果: 90%
```

### 场景 2：高质量处理

```
1. 打开配置面板
2. 选择 "⭐⭐⭐⭐⭐ 极致质量" 预设
3. 查看配置信息显示的强度
4. 关闭面板
5. 开始处理

效果: 95%+
处理时间: 较长
```

### 场景 3：自定义参数

```
1. 打开配置面板
2. 点击数字框输入自定义值
   邻域半径: 20
   平滑迭代: 5
3. 实时查看配置信息的强度指示
4. 关闭面板处理
```

## 💡 技巧

### 技巧 1：隐藏滚动条
```
悬浮面板内容可以滚动
但看不到滚动条
更清爽的视觉体验
```

### 技巧 2：快速预设
```
一键应用完整配置
不用逐个调整参数
节省时间
```

### 技巧 3：实时强度显示
```
当前配置信息显示：
🟢 中等
🟡 很强
🔴 非常强
🔥 超强

直观了解参数强度
```

### 技巧 4：警告提示
```
当参数超过阈值时显示：
⚠️ 警告: 参数非常强，处理时间会很长！

避免意外的长时间处理
```

## 🔍 面板特性详解

### 打开动画
```
0.3 秒钟平滑滑入
从右侧 → 屏幕位置
视觉流畅舒适
```

### 关闭按钮
```
右上角 [✕] 按钮
悬停时旋转 + 变亮
点击立即关闭
```

### 内容滚动
```
内容过多可向下滚动
滚动条自动隐藏
只显示内容，不显示滚动条
```

### 响应式适配
```
> 768px:  右侧悬浮，宽 420px
≤ 768px:  底部悬浮，宽自适应
```

## 📱 移动设备体验

### 小屏幕上的面板
```
从底部弹出
宽度自动适应
高度最大 50vh
不会超出屏幕
```

### 操作方式相同
```
打开: 点击 "► 显示高级配置"
调整: 滑块、数字框、预设
关闭: 点击 [✕] 按钮
```

## 🎨 视觉设计

### 配色方案
```
头部: 渐变背景 (#667eea → #764ba2)
文字: 白色
按钮: 白色半透明
```

### 交互反馈
```
按钮悬停: 背景变亮 + 旋转
面板打开: 平滑滑入
面板关闭: 平滑滑出
```

## ✅ 完整工作流

```
1. 刷新页面
   ↓
2. 上传视频
   ↓
3. 按 Ctrl/Cmd 框选水印
   ↓
4. 点击 "► 显示高级配置"
   → 悬浮面板打开 ✨
   ↓
5. 选择预设或调整参数
   ↓
6. 点击 [✕] 关闭面板
   ↓
7. 点击 "开始处理视频"
   ↓
8. 等待处理完成
   ↓
9. 下载处理后的视频
```

## 🆘 常见问题

### Q: 面板打不开？
**A**: 检查 "► 显示高级配置" 按钮是否可见（需要上传视频后）

### Q: 面板内容看不全？
**A**: 尝试向下滚动，内容会自动滚动（无滚动条显示）

### Q: 移动设备上面板在哪？
**A**: 在屏幕底部，如果看不到向下滑动屏幕

### Q: 如何快速关闭面板？
**A**: 点击面板右上角的 [✕] 按钮

## 🎯 推荐使用步骤

### 首次使用
```
1. 打开面板看看布局
2. 尝试调整一个参数
3. 观察实时反馈
4. 选择预设或自定义
5. 关闭面板开始处理
```

### 熟练使用
```
1. 快速点击预设
2. 微调参数（如需要）
3. 关闭面板
4. 处理视频
```

---

**现在就试试吧！** 🚀

点击 "► 显示高级配置" 打开悬浮面板，体验全新的配置体验！

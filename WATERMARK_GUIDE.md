# 视频水印去除工具 - 使用指南

## 功能特性

### ✨ 核心功能
- **视频上传** - 支持拖拽上传或点击选择本地视频文件
- **实时预览** - 上传后立即预览视频内容
- **水印框选** - 在视频上灵活框选水印所在的区域
- **多位置支持** - 可同时标记多个水印区域
- **智能去除** - 使用高斯模糊和内容感知填充算法
- **结果对比** - 处理前后视频对比预览
- **一键下载** - 处理完成后下载清晰的视频

### 📋 支持的视频格式
- MP4 (.mp4)
- WebM (.webm)
- Ogg (.ogv)
- 其他浏览器支持的视频格式

## 使用步骤

### 1️⃣ 上传视频
- **拖拽上传**：将视频文件直接拖入灰色上传框
- **点击上传**：点击上传框选择本地文件
- 确认文件大小合理（建议≤500MB）

### 2️⃣ 预览和框选水印

#### 框选方式
1. 点击视频播放器下方的"开始处理"前，先框选水印区域
2. 在视频上**按住鼠标左键**并拖拽，形成矩形框
3. 虚线框（绿色）表示当前选择的水印区域

#### 查看选择信息
- 当前选择会显示位置坐标和尺寸
- 可点击"清除当前选择"重新框选

### 3️⃣ 添加多个水印区域

1. 完成第一个水印框选后，点击"确认添加水印区域"
2. 右侧列表会显示已添加的区域（用不同颜色标记）
3. 重复步骤 2-3 继续添加其他水印区域
4. 每个区域可单独删除

### 4️⃣ 处理视频

1. 确认所有水印区域已标记完成
2. 点击"开始处理视频"按钮
3. 系统会逐帧处理视频，显示实时进度
4. 处理时间取决于视频长度和分辨率

#### ⏱️ 处理时间参考
- 1分钟 1080p视频 → 约2-5分钟
- 1分钟 720p视频 → 约1-3分钟
- 1分钟 480p视频 → 约30-60秒

### 5️⃣ 下载结果

1. 处理完成后，会显示"处理结果对比"
2. 可在结果区域对比原始视频和处理后的视频
3. 点击"⬇️ 下载处理后的视频"按钮下载
4. 文件名为 `video-watermark-removed.webm`

## 技术细节

### 水印去除算法

#### 1. 高斯模糊算法（推荐用于简单水印）
```
- 分析水印区域周围像素
- 计算加权平均颜色
- 用平均颜色填充水印区域
- 效果：自然，适合半透明水印
```

#### 2. 内容感知填充（推荐用于复杂水印）
```
- 从水印边界向内扩展
- 迭代多次逐步填充
- 更好地保留背景纹理
- 效果：更自然，但处理时间更长
```

#### 3. 复制填充（快速模式）
```
- 从周围像素随机采样
- 快速填充水印区域
- 效果：速度快，但可能产生伪影
```

### 视频编码格式
- **输出格式**：WebM (VP8 编码 + Opus 音频)
- **比特率**：5Mbps（平衡质量和文件大小）
- **帧率**：保持原视频帧率

## 常见问题

### Q: 处理后的视频质量如何？
A: 质量取决于原始视频和水印复杂度。简单水印效果很好，复杂纹理水印可能需要多次调整框选区域。

### Q: 支持哪些水印类型？
A: 
- ✅ 文字水印（效果最好）
- ✅ Logo水印（效果好）
- ✅ 半透明水印（效果一般）
- ⚠️ 动态水印（需要逐帧标记）

### Q: 如何获得最佳效果？
1. 确保水印框选区域准确覆盖整个水印
2. 可稍微扩大框选区域以包含水印影响区域
3. 对于复杂背景，可能需要多次尝试

### Q: 处理时间为什么这么长？
- 实时逐帧处理需要时间
- 更高分辨率的视频处理时间更长
- 多个水印区域会增加处理时间

### Q: 能否处理很大的视频文件？
- 建议视频大小不超过 500MB
- 超大文件可能导致浏览器内存不足
- 可将长视频分段处理

## 浏览器兼容性

| 浏览器 | 支持 | 版本要求 |
|--------|------|---------|
| Chrome | ✅ | 51+ |
| Firefox | ✅ | 54+ |
| Safari | ✅ | 14.1+ |
| Edge | ✅ | 79+ |

**注意**：需要现代浏览器支持：
- Canvas API
- MediaRecorder API
- VideoCodec 支持

## 隐私说明

- ✅ 所有处理在本地完成
- ✅ 不上传任何文件到服务器
- ✅ 处理完成后可选择性删除临时文件

## 性能优化建议

### 对于大文件
1. 降低视频分辨率（缩放到 720p）
2. 将视频分段处理
3. 使用功能更强的计算机

### 对于长视频
1. 分段处理（每段5-10分钟）
2. 使用专业视频编辑软件作为替代

## 技术栈

- Vue 3 - 前端框架
- Canvas API - 图像处理
- MediaRecorder API - 视频编码
- WebM Codec - 视频格式

## 使用建议

### ✨ 最佳实践
1. **精确框选**：水印框选越精确，效果越好
2. **多区域处理**：如有多个水印，分别标记
3. **预览检查**：处理前仔细检查框选范围
4. **尝试多种算法**：对于复杂水印，可多次尝试

### ⚠️ 注意事项
1. 不要标记过大区域（包含背景内容）
2. 确保水印区域完全在视频范围内
3. 处理中不要关闭浏览器
4. 保留原始文件作为备份

## 联系方式

如有问题或建议，欢迎反馈！

---

**版本**：1.0  
**最后更新**：2026年7月12日

# 水印无法去除 - 快速排查表

## 🎯 按照这个表格逐步排查

| # | 检查项 | 期望结果 | 实际结果 | 状态 |
|---|--------|---------|---------|------|
| 1 | 视频能正常播放吗 | 是 | ___ | ☐ |
| 2 | 能正常框选水印吗 | 右侧显示框选区域 | ___ | ☐ |
| 3 | 框选范围覆盖整个水印吗 | 是 | ___ | ☐ |
| 4 | OpenCV 是否已加载 | Console 显示成功 | ___ | ☐ |
| 5 | 开始处理后有进度条吗 | 进度条 0% → 100% | ___ | ☐ |
| 6 | 处理是否完成 | 显示"✅ 完成" | ___ | ☐ |
| 7 | 对比视频显示吗 | 能看到原始和处理后 | ___ | ☐ |
| 8 | 水印是否消失 | 是/基本消失 | ___ | ☐ |

## 🔴 水印无法去除的原因排序

### 优先级 1（最常见）

**原因：框选不准确**
- 症状：水印只淡化没消失，或完全没变化
- 解决：删除框选，重新框选，**稍微扩大范围**（多包含 5-10px）

**原因：框选范围太小**
- 症状：水印边缘仍然可见
- 解决：重新框选，确保完全覆盖整个水印

### 优先级 2（常见）

**原因：OpenCV 未加载**
- 症状：处理后效果差（70% vs 95%）
- 检查：`console.log(window.cv)` 
- 解决：清除缓存，硬刷新

**原因：使用了备用算法**
- 症状：效果 70-80%，不是 95%
- 检查：Console 显示 "使用备用算法"
- 解决：检查 OpenCV 是否加载；备用算法本身效果就是这样

### 优先级 3（不常见）

**原因：视频未完全加载**
- 症状：处理卡住或跳过大量帧
- 解决：等待视频完全加载再处理

**原因：浏览器不支持**
- 症状：Console 有 Canvas API 错误
- 解决：更换浏览器（Chrome/Firefox/Safari）

**原因：系统资源不足**
- 症状：处理速度非常慢或卡住
- 解决：关闭其他应用，释放内存

## 🧪 快速测试

### 测试 1：检查水印数据

```javascript
// 在 Console 中粘贴运行：
console.log('水印数据:', watermarks.value)
console.log('水印数量:', watermarks.value.length)
```

**应该看到**：
```
水印数据: Array(1)
0: {x: 100, y: 50, width: 300, height: 100, color: "#ff0000"}
水印数量: 1
```

### 测试 2：检查 OpenCV

```javascript
// 在 Console 中粘贴运行：
if (window.cv && window.cv.inpaint) {
  console.log('✅ OpenCV 已完全加载')
} else if (window.cv) {
  console.log('⚠️ OpenCV 部分加载')
} else {
  console.log('❌ OpenCV 未加载')
}
```

### 测试 3：检查视频

```javascript
// 在 Console 中粘贴运行：
const video = document.querySelector('video')
console.log('视频宽:', video?.videoWidth)
console.log('视频高:', video?.videoHeight)
console.log('视频时长:', video?.duration, '秒')
console.log('能否播放:', video?.canPlayType('video/mp4'))
```

## 📝 故障现象匹配表

找到你的现象，按推荐方案处理：

| 现象 | 原因 | 解决方案 |
|------|------|---------|
| 框选后右侧看不到 | 快捷键不对 | 按住 Ctrl(Win) 或 Cmd(Mac) |
| 处理按钮无反应 | 没有框选 | 先框选水印 |
| 处理进度卡住 | 帧加载超时 | 等待或尝试短视频 |
| 完全没有变化 | 框选范围不对 | 重新框选，扩大范围 |
| 效果只有 70-80% | OpenCV 未加载 | 清除缓存，硬刷新 |
| 边缘有伪影 | 框选太紧 | 框选范围再大 5-10px |
| 某些区域没处理 | 有多个水印未框选 | 分别框选每个水印 |

## ✅ 成功标准

满足以下条件说明修复成功：

- [x] 能上传视频（✅ 完成）
- [x] 能框选水印（✅ 完成）
- [ ] 能处理视频
- [ ] 水印基本消失或明显淡化（70% 以上）
- [ ] 处理时间合理（不超过 30 分钟/分钟视频）

## 🆘 紧急求助模板

如果无法解决，提供以下信息：

```
1. 现象描述：
   [请描述你看到的现象]

2. Console 错误信息：
   [粘贴 F12 Console 中的错误]

3. 检查结果：
   - OpenCV 加载：是/否
   - 水印框选：成功/失败
   - 处理进度：0%/卡住
   - 最终效果：无变化/部分变化/完全消失

4. 环境信息：
   - 浏览器：Chrome/Firefox/Safari/Edge
   - 系统：Windows/Mac/Linux
   - 视频分辨率：1920x1080/其他
   - 视频大小：XXX MB
```

---

## 🚀 一句话快速排查

**症状 → 解决**

- 框选不显示 → 按住 Ctrl/Cmd
- 处理按钮无反应 → 先框选水印
- 水印没消失 → 重新框选，**扩大范围**
- 效果不好 → 检查 OpenCV 是否加载
- 完全不工作 → 硬刷新 (Ctrl+Shift+R) 清除缓存

---

**最后记住**：90% 的问题都是因为框选不准确！

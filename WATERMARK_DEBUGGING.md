# 水印无法去除 - 完整诊断指南

## 🔍 问题诊断步骤

### 步骤 1：打开浏览器控制台

1. **按 F12 打开开发者工具**
2. **切换到 Console 标签**
3. **刷新页面**
4. **查看是否有错误信息**

### 步骤 2：检查水印是否正确框选

**症状**：框选后看不到水印区域

**排查**：
```
1. 在视频上按住 Ctrl/Cmd
2. 拖拽框选水印区域
3. 右侧应该出现 "已标记水印区域"
4. 如果不出现，检查是否正确按住 Ctrl/Cmd
```

**控制台检查**：
```javascript
// 在 Console 中输入：
console.log(watermarks)  // 应该显示数组，包含框选的区域
```

### 步骤 3：检查水印数据格式

框选的水印应该包含这些属性：
```javascript
{
  x: 100,        // 水印左边界
  y: 50,         // 水印上边界
  width: 300,    // 水印宽度
  height: 100,   // 水印高度
  color: "#ff0000"  // 框选时的颜色
}
```

**检查方法**：
```javascript
// Console 中输入
console.log(watermarks.value)
```

应该看到类似：
```
Array(1)
0: {x: 100, y: 50, width: 300, height: 100, color: "#ff0000"}
```

## ❌ 常见问题和解决方案

### 问题 1：框选后水印没有显示

**原因**：
- 按键不对（应该是 Ctrl 而不是其他键）
- 框选范围太小
- 视频还未加载完

**解决**：
```
1. 确保视频已完全加载（可以播放）
2. 按住 Windows/Linux: Ctrl 或 Mac: Cmd
3. 在视频上拖拽，范围要包含整个水印
4. 松开鼠标完成框选
5. 检查右侧是否显示框选结果
```

### 问题 2：点击"开始处理"后没有反应

**排查**：
```javascript
// Console 中查看是否有错误
console.log('水印数量:', watermarks.value.length)  // 应该 > 0
console.log('OpenCV 可用:', cv.value ? '是' : '否')
```

**可能的原因**：
- 没有框选水印（watermarks.length === 0）
- 视频未加载（video.src 为空）
- 浏览器不支持 Canvas API

### 问题 3：处理进度卡住

**排查**：
```javascript
// 检查帧加载是否正常
// 查看 Console 中是否有日志输出
```

**解决**：
```
1. 检查网络连接
2. 尝试较短的视频（< 30 秒）
3. 降低视频分辨率
4. 清除浏览器缓存后重试
```

### 问题 4：处理完成但水印仍然存在

**最可能的原因**：

#### 原因 A：框选不精确

**症状**：水印的某些部分仍然可见

**解决**：
```
1. 删除现有框选
2. 重新框选，稍微扩大范围
3. 确保完全覆盖水印
4. 重新处理
```

#### 原因 B：OpenCV.js 未加载

**排查**：
```javascript
// Console 中输入
console.log(window.cv)     // 应该是一个对象
console.log(window.cv?.Mat) // 应该是一个函数
```

如果看到 `undefined`，说明 OpenCV 未加载。

**解决**：
```
1. 查看 Network 标签，检查 opencv.js 是否加载成功
2. 如果显示 403 或其他错误，清除缓存
3. 硬刷新页面 (Ctrl+Shift+R)
4. 等待 5-10 秒让 OpenCV 加载
```

#### 原因 C：备用算法效果不佳

**症状**：处理后水印变淡但不消失

**这是正常的**：
- 备用算法效果 70-80%
- OpenCV 算法效果 95%+
- 如果无法加载 OpenCV，只能使用备用算法

**解决**：
```
1. 检查 OpenCV 是否已加载
2. 如果已加载但仍不理想，调整框选范围
3. 框选时稍微扩大范围（多包含 5-10px）
```

## 🛠️ 高级调试

### 启用详细日志

在 `processVideo` 函数中已添加详细的日志。

**查看日志**：
1. 按 F12 打开 Console
2. 点击"开始处理视频"
3. 观察日志输出

**日志示例**：
```
========== 开始处理视频 ==========
水印数量: 1
水印 0: {x: 100, y: 50, width: 300, height: 100}
视频尺寸: 1920 x 1080
OpenCV 可用: 是
处理第 0 帧，水印数: 1
处理第 1 帧，水印数: 1
处理第 2 帧，水印数: 1
...
开始合成 XXX 帧...
========== 处理完成 ==========
```

### 检查 OpenCV 状态

```javascript
// Console 中执行

// 检查 OpenCV 是否已加载
if (window.cv && window.cv.Mat) {
  console.log('✅ OpenCV 已加载')
  console.log('可用方法:', Object.keys(window.cv).slice(0, 10))
} else {
  console.log('❌ OpenCV 未加载')
}

// 检查掩码创建
const mask = window.cv.Mat.zeros(100, 100, window.cv.CV_8UC1)
console.log('✅ 可以创建 Mat 对象')
mask.delete()

// 检查 inpaint 函数
if (window.cv.inpaint) {
  console.log('✅ inpaint 函数可用')
} else {
  console.log('❌ inpaint 函数不可用')
}
```

## 📊 逐步调试流程

### 流程图

```
上传视频
   ↓
视频能否播放？
   ├─ 否 → 检查视频格式
   └─ 是 → 继续
   ↓
框选水印
   ↓
右侧能否显示框选区域？
   ├─ 否 → 检查快捷键 (Ctrl/Cmd)
   └─ 是 → 继续
   ↓
点击"开始处理"
   ↓
处理进度是否正常？
   ├─ 卡住 → 等待或查看 Console 错误
   └─ 正常 → 继续
   ↓
处理是否完成？
   ├─ 失败 → 查看错误信息
   └─ 成功 → 继续
   ↓
对比原始和处理后的视频
   ↓
水印是否消失？
   ├─ 是 → ✅ 成功！
   └─ 否 → 重新框选，扩大范围
```

## 🧪 测试场景

### 场景 1：纯色背景 + 文字水印（最容易）

```
预期效果：⭐⭐⭐⭐⭐ 95%+
- 完全看不见水印
- 背景完全自然
```

### 场景 2：彩色背景 + Logo（容易）

```
预期效果：⭐⭐⭐⭐ 85-90%
- 水印大幅淡化或消失
- 背景基本自然
```

### 场景 3：复杂纹理背景 + 半透明水印（困难）

```
预期效果：⭐⭐⭐ 70-75%
- 水印明显淡化但可能仍可见
- 这是正常的，属于偏难的场景
```

## 📋 完整排查清单

处理水印去除问题时，按顺序检查：

### 基础检查
- [ ] 视频能否正常上传和播放
- [ ] 视频格式是否支持（MP4、WebM 等）
- [ ] 视频不超过 500MB

### 框选检查
- [ ] 是否按住快捷键（Ctrl 或 Cmd）
- [ ] 是否正确拖拽了框选区域
- [ ] 右侧是否显示"已标记水印区域"
- [ ] 框选范围是否覆盖整个水印

### 处理检查
- [ ] 是否点击了"开始处理视频"按钮
- [ ] 处理进度是否显示进展
- [ ] Console 中是否有错误信息
- [ ] 是否等待了足够的时间

### 结果检查
- [ ] 处理是否成功完成
- [ ] 是否显示"✅ 视频处理完成"
- [ ] 能否看到处理前后的对比
- [ ] 水印是否有明显淡化

### 环境检查
- [ ] OpenCV.js 是否已加载（查看 Network 标签）
- [ ] 浏览器是否最新版本
- [ ] 是否有网络连接
- [ ] 系统内存是否充足

## 🆘 如果仍无法解决

### 收集诊断信息

1. **截图 Console 的错误信息**
2. **记录水印框选的坐标**
3. **记录视频分辨率和大小**
4. **记录浏览器和系统信息**

### 尝试以下操作

1. **清除所有缓存**
   ```
   Ctrl+Shift+Delete → 清除所有时间 → 清除
   ```

2. **尝试其他浏览器**
   - Chrome → Firefox → Safari → Edge

3. **降低视频质量**
   ```bash
   # 使用 FFmpeg 降低分辨率
   ffmpeg -i input.mp4 -vf scale=1280:-1 output.mp4
   ```

4. **使用更简单的测试视频**
   - 纯色背景
   - 大号文字水印
   - 短视频（< 10 秒）

## ✅ 验证修复成功

修复成功的标志：

- [ ] Console 显示 "✅ OpenCV.js 加载成功" 或 "ℹ️ 将使用备用算法"
- [ ] 可以正常框选水印
- [ ] 处理进度条正常显示
- [ ] 处理完成后显示成功提示
- [ ] 对比视频中，水印明显淡化或消失

---

**关键提示**：

🎯 **最常见的原因**：框选不够准确或范围太小

💡 **快速解决方案**：
1. 重新框选，稍微扩大范围
2. 确保完全覆盖水印
3. 重新处理

如果以上都不行，查看 Console 中的具体错误信息，这通常能指出真实问题所在。
